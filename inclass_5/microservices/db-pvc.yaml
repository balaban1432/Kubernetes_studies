apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
spec:
  storageClassName: manuel
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi